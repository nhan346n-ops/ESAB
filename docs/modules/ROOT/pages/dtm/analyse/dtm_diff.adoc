= Compute difference between two DTM

== Overview

This application takes the first file as reference file, the definition from the second files is adapted to fix resolution and bounding box of the reference file.

Both are substracted : result = reference file - secondary file

Then the result is saved as a geotiff into the given output directory

include::example$generated/dtm/analyse/diff_mnt_params.adoc[]
